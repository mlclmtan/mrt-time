# Neon Postgres connection string (pooled). Required by the serverless functions.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Bearer token guarding write access to POST /api/observations.
# If unset, the write endpoint is disabled and the app is read-only.
ADMIN_TOKEN=change-me
