{
  "name": "mrt-time",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MRT next-train countdown, predicted from timed board photos. Starting with the Thomson–East Coast Line (TEL).",
  "scripts": {
    "migrate": "node scripts/migrate.mjs",
    "dev": "vercel dev"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "ws": "^8.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
